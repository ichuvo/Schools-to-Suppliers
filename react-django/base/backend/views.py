from django.shortcuts import render, get_object_or_404

# import the models
from .models import Product

# Python function that takes a web request and returns a
# web response. This response can be the HTML contents
# of a Web page, or a redirect, or a 404 error,
# or an XML document, or an image, etc

# Use python manage.py shell to add entries


# Create your views here.

# def product_detail(request, pk):
#    product = get_object_or_404(Product, pk = pk)
#    return render(request,'product_detail.html', ('product:' product))
